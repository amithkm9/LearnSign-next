# Core AI service (chat, voice, report)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
openai>=1.55.0
pydantic>=2.9.0
python-dotenv>=1.0.0

# Sign recognition (heavy — lazy-loaded). Requires Python <= 3.12.
# Pinned set: these versions of tensorflow/mediapipe/protobuf are mutually
# compatible, and the MediaPipe `solutions` API still exists at 0.10.14.
# The .h5 model is loaded with TF 2.16's default Keras 3 (recognition.py does
# NOT set TF_USE_LEGACY_KERAS); tf-keras is pinned only for TF 2.16 compat.
tensorflow==2.16.2
tf-keras==2.16.0
mediapipe==0.10.14
protobuf>=4.25.3,<5
opencv-python
numpy<2
